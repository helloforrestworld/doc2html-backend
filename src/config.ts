export const connectionStr = 'mongodb://127.0.0.1:27017/doctool';

export const apiPrefix = '/api';

export const port = 3000;

export const domain = 'localhost';

export const cdnPrefix = `http://${domain}:${port}`;
